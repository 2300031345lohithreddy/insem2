apiVersion: v1
kind: Service
metadata:
  name: {{ include "s104.fullname" . }}
  labels:
    {{- include "s104.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "s104.selectorLabels" . | nindent 4 }}
